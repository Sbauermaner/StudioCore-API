# Полная схема цепочек анализа цветов в StudioCore

## 📊 Общая форма анализа

```
ТЕКСТ
  ↓
[СЛОВАРИ: LOVE_WORDS, PAIN_WORDS, TRUTH_WORDS]
  ↓
TLP (Truth, Love, Pain)
  ↓
[СЛОВАРЬ: KEY_COLOR_PALETTE]
  ↓
ЦВЕТ TLP (primary_color, accent_color)
  ↓
[СРАВНЕНИЕ С: EMOTION_COLOR_MAP]
  ↓
ЭМОЦИЯ (определяется по цвету TLP)
  ↓
[СЛОВАРЬ: EMOTION_COLOR_MAP]
  ↓
ЦВЕТ ЭМОЦИИ (color_wave из эмоции)
  ↓
[СРАВНЕНИЕ С: Цвета жанров лирики]
  ↓
ЦВЕТ ЖАНРА ЛИРИКИ
  ↓
[СЛОВАРЬ: EMOTION_TO_VOCAL_MAP → EMOTION_COLOR_MAP]
  ↓
ЦВЕТ ВОКАЛА (для каждой секции)
  ↓
[СРАВНЕНИЕ С: Цвета жанров музыки]
  ↓
ЦВЕТ ЖАНРА МУЗЫКИ
  ↓
[АГРЕГАЦИЯ ВСЕХ ЦВЕТОВ]
  ↓
ОБЩИЙ COLOR_WAVE
```

---

## 🔗 Детальная последовательность

### Шаг 1: Текст → TLP

**Словари:**
- `LOVE_WORDS` (98 слов)
- `PAIN_WORDS` (54 слова)
- `TRUTH_WORDS` (89 слов)

**Модуль:** `studiocore.tlp_engine.TruthLovePainEngine`

**Результат:**
```python
tlp = {
    "truth": 1.000,
    "love": 0.000,
    "pain": 0.000,
    "conscious_frequency": 1.000
}
```

**Статус:** ✅ Работает

---

### Шаг 2: TLP → Цвет

**Словарь:** `KEY_COLOR_PALETTE` (13 ключей)

**Модуль:** `studiocore.color_engine_adapter.ColorEngineAdapter`

**Метод:** `resolve_color_wave()`

**Логика:**
- TLP Truth → indigo/violet цвета
- TLP Love → pink/red цвета
- TLP Pain → crimson/dark цвета

**Результат:**
```python
color_profile = {
    "primary_color": "#40E0D0",  # turquoise (из peace/truth)
    "accent_color": "#FFFFFF"     # white
}
```

**Статус:** ✅ Работает

---

### Шаг 3: Цвет → Эмоция

**Словарь:** `EMOTION_COLOR_MAP` (93 эмоции)

**Функция:** `get_emotion_colors()`

**Логика:**
1. Берем `primary_color` из TLP
2. Ищем этот цвет в `EMOTION_COLOR_MAP`
3. Находим эмоции с таким цветом
4. Определяем доминирующую эмоцию

**Пример:**
- Цвет TLP: `#40E0D0` (turquoise)
- Найден в эмоциях: `['peace', 'awe', 'wonder']`
- Доминирующая эмоция: `peace` (0.600)

**Результат:**
```python
emotion = {
    "dominant": "peace",
    "profile": {
        "peace": 0.600,
        "joy": 0.300
    }
}
```

**Статус:** ✅ Работает (частично - цвет TLP найден в эмоциях)

---

### Шаг 4: Цвет эмоции → Цвет жанра лирики

**Словарь:** Нужен словарь цветов для жанров лирики

**Модуль:** `studiocore.genre_weights.GenreWeightsEngine`

**Логика (текущая):**
- Цвет эмоции используется в `score_domains()` для коррекции весов доменов
- Цвета эмоций влияют на выбор домена (lyrical, hard, electronic, soft, cinematic)

**Логика (ожидаемая):**
1. Берем цвет эмоции (например, `#40E0D0` для `peace`)
2. Сравниваем с цветами жанров лирики
3. Находим наиболее подходящий жанр лирики
4. Берем цвет этого жанра

**Пример:**
- Цвет эмоции: `#40E0D0` (peace)
- Жанр лирики: `lyrical_song`
- Цвет жанра лирики: `#FFC0CB` (из GENRE_DATABASE.md)

**Результат:**
```python
lyrical_genre_color = {
    "genre": "lyrical_song",
    "color": "#FFC0CB"  # Нужен словарь
}
```

**Статус:** ⚠️ Частично работает (цвет влияет на выбор жанра, но нет прямого сравнения)

---

### Шаг 5: Цвет лирики → Цвет вокала

**Словарь:** `EMOTION_TO_VOCAL_MAP` (78 эмоций)

**Функция:** `get_vocal_for_section()` → `get_emotion_colors()`

**Логика:**
1. Берем эмоцию секции
2. Находим вокал через `EMOTION_TO_VOCAL_MAP[emotion]`
3. Берем цвет эмоции через `get_emotion_colors(emotion)`
4. Передаем цвет в секцию

**Пример:**
- Эмоция секции: `neutral`
- Вокал: `tenor` (из `EMOTION_TO_VOCAL_MAP["neutral"]`)
- Цвет вокала: `['#FFFFFF', '#B0BEC5', '#ECEFF1']` (из `EMOTION_COLOR_MAP["neutral"]`)

**Результат:**
```python
lyrics_sections[0] = {
    "emotion": "neutral",
    "vocal_technique": "tenor",
    "color_wave": ["#FFFFFF", "#B0BEC5", "#ECEFF1"]
}
```

**Статус:** ✅ Работает

---

### Шаг 6: Цвет вокала → Цвет жанра музыки

**Словарь:** Нужен словарь цветов для жанров музыки

**Модуль:** `studiocore.genre_weights.GenreWeightsEngine`

**Логика (текущая):**
- Цвета эмоций влияют на выбор музыкального жанра через `score_domains()`
- Цвета вокала не сравниваются напрямую с цветами жанров музыки

**Логика (ожидаемая):**
1. Берем цвет вокала (например, `#FFFFFF` для `neutral`)
2. Сравниваем с цветами жанров музыки
3. Находим наиболее подходящий жанр музыки
4. Берем цвет этого жанра

**Пример:**
- Цвет вокала: `#FFFFFF` (neutral)
- Жанр музыки: `lyrical_song`
- Цвет жанра музыки: `#FFC0CB` (из GENRE_DATABASE.md)

**Результат:**
```python
music_genre_color = {
    "genre": "lyrical_song",
    "color": "#FFC0CB"  # Нужен словарь
}
```

**Статус:** ⚠️ Частично работает (цвет влияет на выбор жанра, но нет прямого сравнения)

---

### Шаг 7: Общий color_wave

**Модуль:** `studiocore.core_v6.StudioCoreV6._backend_analyze()`

**Логика (текущая):**
1. Берем `color_wave` из `color_profile.get("wave")`
2. Если нет, берем из `color_res.colors` (ColorEngineAdapter)
3. Если нет, создаем из `primary_color` и `accent_color`

**Логика (ожидаемая):**
1. Собираем все цвета из цепочки:
   - Цвет TLP
   - Цвет эмоции
   - Цвет жанра лирики
   - Цвет вокала (для каждой секции)
   - Цвет жанра музыки
2. Агрегируем в общий `color_wave`
3. Передаем в `style.color_wave`

**Результат:**
```python
style = {
    "color_wave": [
        "#40E0D0",  # Цвет TLP (peace)
        "#E0F7FA",  # Цвет эмоции (peace)
        "#FFFFFF",  # Цвет жанра лирики (lyrical_song)
        "#FFC0CB",  # Цвет вокала (tenor)
        "#FFD93D"   # Цвет жанра музыки (lyrical_song)
    ]
}
```

**Статус:** ✅ Работает (частично - формируется, но не агрегирует все цвета)

---

## 📋 Словари, которые используются

### ✅ Используются:

1. **TLP словари:**
   - `LOVE_WORDS` (98 слов)
   - `PAIN_WORDS` (54 слова)
   - `TRUTH_WORDS` (89 слов)

2. **Цветовые словари:**
   - `KEY_COLOR_PALETTE` (13 ключей)
   - `EMOTION_COLOR_MAP` (93 эмоции)

3. **Вокальные словари:**
   - `EMOTION_TO_VOCAL_MAP` (78 эмоций)

4. **Жанровые словари:**
   - `GlobalGenreRegistry.music_genres` (268 жанров)
   - `GlobalGenreRegistry.lyrical_genres` (53 жанра)

### ⚠️ Отсутствуют:

1. **Словарь цветов для жанров лирики:**
   - Нужен маппинг: `lyrical_genre → color`
   - Пример: `lyrical_song → #FFC0CB`

2. **Словарь цветов для жанров музыки:**
   - Нужен маппинг: `music_genre → color`
   - Пример: `lyrical_song → #FFC0CB`

---

## 🔧 Что нужно реализовать

### 1. Создать словарь цветов для жанров лирики

```python
LYRICAL_GENRE_COLORS = {
    "lyrical_song": ["#FFC0CB", "#FFB6C1", "#FFE4E1"],
    "ballad": ["#D8BFD8", "#E6E6FA", "#C3B1E1"],
    "ode": ["#8A2BE2", "#4B0082", "#FF00FF"],
    "sonnet": ["#40E0D0", "#E0F7FA", "#FFFFFF"],
    # ... и т.д.
}
```

### 2. Создать словарь цветов для жанров музыки

```python
MUSIC_GENRE_COLORS = {
    "lyrical_song": ["#FFC0CB", "#FFB6C1", "#FFE4E1"],
    "gothic_poetry": ["#2C1A2E", "#1B1B2F", "#000000"],
    "cinematic": ["#8A2BE2", "#4B0082", "#FF00FF"],
    # ... и т.д.
}
```

### 3. Реализовать сравнение цветов

```python
def compare_colors(color1: str, color2: str) -> float:
    """Сравнивает два цвета и возвращает схожесть (0.0-1.0)"""
    # Используем цветовое расстояние (например, Euclidean distance в RGB)
    pass

def find_matching_genre_by_color(target_color: str, genre_colors: Dict[str, List[str]]) -> str:
    """Находит жанр с наиболее похожим цветом"""
    # Сравниваем target_color с цветами каждого жанра
    # Возвращаем жанр с максимальной схожестью
    pass
```

### 4. Реализовать агрегацию всех цветов

```python
def aggregate_colors(
    tlp_color: str,
    emotion_color: str,
    lyrical_genre_color: str,
    vocal_colors: List[str],
    music_genre_color: str
) -> List[str]:
    """Агрегирует все цвета в общий color_wave"""
    # Собираем все уникальные цвета
    # Сортируем по весу/важности
    # Возвращаем список цветов
    pass
```

---

## 📊 Текущая реализация vs Ожидаемая

| Шаг | Текущая реализация | Ожидаемая реализация | Статус |
|-----|-------------------|---------------------|--------|
| 1. Текст → TLP | ✅ Работает | ✅ Работает | ✅ |
| 2. TLP → Цвет | ✅ Работает | ✅ Работает | ✅ |
| 3. Цвет → Эмоция | ⚠️ Частично | ✅ Полностью | ⚠️ |
| 4. Цвет эмоции → Цвет жанра лирики | ⚠️ Влияет на выбор жанра | ✅ Прямое сравнение | ⚠️ |
| 5. Цвет лирики → Цвет вокала | ✅ Работает | ✅ Работает | ✅ |
| 6. Цвет вокала → Цвет жанра музыки | ⚠️ Влияет на выбор жанра | ✅ Прямое сравнение | ⚠️ |
| 7. Общий color_wave | ⚠️ Частично | ✅ Полная агрегация | ⚠️ |

---

## 🎯 Итоговые рекомендации

1. ✅ **Все словари используются** для анализа текста
2. ⚠️ **Нужно создать** словари цветов для жанров лирики и музыки
3. ⚠️ **Нужно реализовать** прямое сравнение цветов между этапами
4. ⚠️ **Нужно улучшить** агрегацию всех цветов в общий `color_wave`

---

## 📝 Пример полной цепочки

```
Текст: "Вы помните, Вы всё, конечно, помните..."

1. TLP: Truth=1.000, Love=0.000, Pain=0.000
   ↓
2. Цвет TLP: #40E0D0 (turquoise)
   ↓
3. Эмоция: peace (цвет #40E0D0 найден в EMOTION_COLOR_MAP["peace"])
   ↓
4. Цвет эмоции: ['#40E0D0', '#E0F7FA', '#FFFFFF']
   ↓
5. Жанр лирики: lyrical_song (выбран на основе цвета)
   Цвет жанра лирики: #FFC0CB (из LYRICAL_GENRE_COLORS)
   ↓
6. Вокал: tenor (из EMOTION_TO_VOCAL_MAP["neutral"])
   Цвет вокала: ['#FFFFFF', '#B0BEC5', '#ECEFF1']
   ↓
7. Жанр музыки: lyrical_song (выбран на основе цвета)
   Цвет жанра музыки: #FFC0CB (из MUSIC_GENRE_COLORS)
   ↓
8. Общий color_wave: [
       '#40E0D0',  # TLP
       '#E0F7FA',  # Эмоция
       '#FFFFFF',  # Жанр лирики
       '#FFC0CB',  # Вокал
       '#FFC0CB'   # Жанр музыки
   ]
```

---

## ✅ Выводы

1. **Все словари используются** для анализа текста ✅
2. **Последовательность работает** частично ⚠️
3. **Нужно добавить** словари цветов для жанров
4. **Нужно реализовать** прямое сравнение цветов
5. **Нужно улучшить** агрегацию всех цветов в общий `color_wave`

