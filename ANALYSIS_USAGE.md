# 📖 Руководство по использованию анализа текста

## 🚀 Быстрый старт

### Вариант 1: Использование скрипта `test_analysis.py`

```bash
# С текстом по умолчанию
python3 test_analysis.py

# С вашим текстом
python3 test_analysis.py "Ваш текст для анализа здесь"
```

### Вариант 2: Использование в коде Python

```python
from studiocore.core_v6 import StudioCoreV6

# Создаем экземпляр ядра
core = StudioCoreV6()

# Анализируем текст
text = """
[Verse 1]
Я вижу красные розы в глубоком синем море
Сердце бьется, как барабан в ночи
"""

result = core.analyze(text)

# Получаем основные выходные данные
fanf = result.get("fanf", {})
style = result.get("style", {})
emotion = result.get("emotion", {})
tlp = result.get("tlp", {})
vocal = result.get("vocal", {})
```

---

## 📊 Структура выходных данных

### Основные блоки результата:

#### 1. **`fanf`** - FANF аннотации и промпты
```python
{
    "style_prompt": "...",      # Промпт для стиля
    "lyrics_prompt": "...",    # Промпт для лирики
    "ui_text": "...",          # Текст с аннотациями для UI
    "full": "...",             # Полный FANF текст
    "summary": "..."           # Краткое резюме
}
```

#### 2. **`style`** - Стилистические параметры
```python
{
    "genre": "rock",           # Жанр
    "bpm": 120,                # BPM
    "key": "C minor",          # Тональность
    "mood": "melancholic",     # Настроение
    "color_wave": ["#...", ...], # Цветовая волна
    "instrumentation": {...}   # Инструментация
}
```

#### 3. **`emotion`** - Эмоциональный профиль
```python
{
    "profile": {
        "joy": 0.5,
        "sadness": 0.7,
        "anger": 0.2,
        ...
    },
    "dynamic_profile": {...},  # Динамический профиль
    "curve": [...],            # Кривая эмоций
    "pivots": [...]            # Точки поворота
}
```

#### 4. **`tlp`** - Truth, Love, Pain
```python
{
    "truth": 0.6,              # Правда
    "love": 0.8,               # Любовь
    "pain": 0.4,               # Боль
    "conscious_frequency": 0.7  # Частота сознания
}
```

#### 5. **`vocal`** - Вокальные характеристики
```python
{
    "gender": "male",          # Пол
    "type": "baritone",        # Тип голоса
    "tone": "warm",            # Тембр
    "style": "emotional",      # Стиль
    "dynamics": {...}          # Динамика
}
```

#### 6. **`structure`** - Структура текста
```python
{
    "sections": [
        {
            "tag": "Verse 1",
            "lines": [...],
            "line_count": 4
        },
        ...
    ],
    "language": {...}          # Языковая информация
}
```

---

## 💡 Примеры использования

### Пример 1: Получить промпт для Suno

```python
from studiocore.core_v6 import StudioCoreV6

core = StudioCoreV6()
result = core.analyze("Ваш текст")

# Промпт для стиля
style_prompt = result["fanf"]["style_prompt"]
print(style_prompt)
# Вывод: [GENRE] [MOOD] [INSTRUMENTATION] [VOCAL] [PRODUCTION] [BPM] [KEY]
```

### Пример 2: Получить эмоциональный профиль

```python
emotion_profile = result["emotion"]["profile"]
dominant_emotion = max(emotion_profile, key=emotion_profile.get)
print(f"Доминирующая эмоция: {dominant_emotion}")
```

### Пример 3: Получить TLP значения

```python
tlp = result["tlp"]
print(f"Truth: {tlp['truth']:.2f}")
print(f"Love: {tlp['love']:.2f}")
print(f"Pain: {tlp['pain']:.2f}")
```

### Пример 4: Получить цветовую гамму

```python
color_wave = result["style"]["color_wave"]
print(f"Цветовая волна: {color_wave}")
```

---

## 🔍 Полный список доступных полей

См. файл `OUTPUT_STRUCTURE.md` для полного описания всех полей результата.

---

## ⚠️ Важные замечания

1. **Статус**: Каждый анализ создает новый набор движков (stateless архитектура)
2. **Производительность**: Анализ может занять несколько секунд для длинных текстов
3. **Формат текста**: Поддерживаются маркеры секций `[Verse 1]`, `[Chorus]` и т.д.
4. **Языки**: Поддерживаются русский и английский языки

---

## 📝 Пример полного вывода

Запустите `test_analysis.py` чтобы увидеть пример полного вывода:

```bash
python3 test_analysis.py "Ваш текст"
```

Или используйте пример по умолчанию:

```bash
python3 test_analysis.py
```

