# Проблемы Процессов - Где Процессы Не Проходят Полностью

Таблицы с указанием строк кода, где процессы не проходят по всем параметрам от начала анализа до выхода.

---

## 1. Таблица: Пропущенные Процессы в analyze()

| Процесс | Должен Вызываться | Фактически Вызывается | Строка Кода | Проблема |
|---------|-------------------|----------------------|------------|----------|
| **TLP анализ** | `self.tlp.analyze(raw)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:536` | TLP передается как `None` в rhythm.analyze() |
| **CF расчет** | Из TLP: `tlp.get("conscious_frequency")` | ❌ НЕ рассчитывается | `monolith_v4_3_1.py:536` | CF передается как `None` в rhythm.analyze() |
| **Style.build()** | `self.style.build(emo, tlp, text, bpm, ...)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:556-565` | Используются FALLBACK значения вместо реальных расчетов |
| **Semantic Layers** | `self._build_semantic_layers(emo, tlp, bpm, key)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:314-396` | Метод существует, но не вызывается в analyze() |
| **Section Analysis** | `self._analyze_sections(text_blocks, preferred_gender)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:258-308` | Метод существует, но не вызывается в analyze() |
| **Vocal Allocator** | `self.vocal_allocator.analyze(...)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:248` | Объект создан, но не используется |
| **Integrity Scan** | `self.integrity.analyze(text)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:230` | Объект создан, но не используется |
| **Hybrid Genre Engine** | `self._hge.resolve(...)` | ❌ НЕ вызывается | `core_v6.py:45` | Объект создан, но не используется |
| **Text Annotation** | `self.annotate_text(text_blocks, section_profiles, semantic_sections)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:402-514` | Метод существует, но не вызывается в analyze() |
| **Color Resolution** | `ColorEngineAdapter.resolve_color_wave(result)` | ❌ НЕ вызывается | - | Color не рассчитывается |

---

## 2. Таблица: Параметры с Fallback Вместо Расчетов

| Параметр | Должен Рассчитываться | Фактически | Строка Кода | Fallback Значение |
|----------|----------------------|-----------|------------|-------------------|
| **TLP** | `self.tlp.analyze(raw)` | ❌ `None` | `monolith_v4_3_1.py:536` | - |
| **CF (Conscious Frequency)** | `tlp.get("conscious_frequency")` | ❌ `None` | `monolith_v4_3_1.py:536` | - |
| **Style Genre** | `self.style.build(...).get("genre")` | ❌ FALLBACK | `monolith_v4_3_1.py:557` | `DEFAULT_CONFIG.FALLBACK_STYLE` |
| **Style Style** | `self.style.build(...).get("style")` | ❌ FALLBACK | `monolith_v4_3_1.py:558` | `DEFAULT_CONFIG.FALLBACK_STYLE` |
| **Style Visual** | `self.style.build(...).get("visual")` | ❌ FALLBACK | `monolith_v4_3_1.py:561` | `DEFAULT_CONFIG.FALLBACK_VISUAL` |
| **Style Narrative** | `self.style.build(...).get("narrative")` | ❌ FALLBACK | `monolith_v4_3_1.py:562` | `DEFAULT_CONFIG.FALLBACK_NARRATIVE` |
| **Style Structure** | `self.style.build(...).get("structure")` | ❌ FALLBACK | `monolith_v4_3_1.py:563` | `DEFAULT_CONFIG.FALLBACK_STRUCTURE` |
| **Structure Layout** | Из semantic_layers или style | ❌ FALLBACK | `monolith_v4_3_1.py:553` | `DEFAULT_CONFIG.FALLBACK_STRUCTURE` |
| **Emotion Dominant** | `emotions.get("dominant")` | ⚠️ Частично | `monolith_v4_3_1.py:564` | `DEFAULT_CONFIG.FALLBACK_EMOTION` (если нет) |
| **Key** | `tone_hint.get("key")` | ⚠️ Частично | `monolith_v4_3_1.py:546-548` | `DEFAULT_CONFIG.FALLBACK_KEY` (если None или "auto") |
| **BPM** | `rhythm_analysis.get("global_bpm")` | ⚠️ Частично | `monolith_v4_3_1.py:537` | `DEFAULT_CONFIG.FALLBACK_BPM` (если нет) |

---

## 3. Таблица: Неполная Обработка Параметров в Rhythm

| Параметр | Передается | Ожидается | Строка Кода | Проблема |
|----------|-----------|-----------|------------|----------|
| **TLP** | `None` | `Dict[str, float]` | `monolith_v4_3_1.py:536` | TLP не рассчитывается, передается None |
| **CF** | `None` | `float` | `monolith_v4_3_1.py:536` | CF не рассчитывается, передается None |
| **Emotions** | ✅ `emotions` | `Dict[str, float]` | `monolith_v4_3_1.py:536` | ✅ Корректно |
| **Header BPM** | ❌ Не передается | `Optional[float]` | `monolith_v4_3_1.py:536` | Header BPM не извлекается явно |

**В rhythm.py:**
- Строка `373`: `emotions = emotions or {}` - обрабатывает None
- Строка `374`: `tlp = tlp or {}` - обрабатывает None, но без TLP расчеты неполные
- Строка `388`: `cf=cf` - если cf=None, TLP коррекция не применяется

---

## 4. Таблица: Неполная Обработка в Style

| Параметр | Должен Передаваться | Фактически | Строка Кода | Проблема |
|----------|---------------------|-----------|------------|----------|
| **Style.build() вызов** | `self.style.build(emo, tlp, text, bpm, ...)` | ❌ НЕ вызывается | `monolith_v4_3_1.py:556` | Вместо вызова используются FALLBACK значения |
| **Emotions** | ✅ Должен передаваться | ❌ Не передается | - | Emotions есть, но не используется для Style |
| **TLP** | ✅ Должен передаваться | ❌ Не рассчитывается | `monolith_v4_3_1.py:536` | TLP не рассчитывается |
| **Text** | ✅ Должен передаваться | ✅ Есть | `monolith_v4_3_1.py:530` | ✅ Корректно |
| **BPM** | ✅ Должен передаваться | ✅ Есть | `monolith_v4_3_1.py:537` | ✅ Корректно |
| **Semantic Hints** | Опционально | ✅ Есть | `monolith_v4_3_1.py:525` | ✅ Корректно |
| **Voice Hint** | Опционально | ❌ Не извлекается | - | Voice hint не извлекается из section_profiles |

---

## 5. Таблица: Пропущенные Методы в analyze()

| Метод | Назначение | Вызывается | Строка Кода | Проблема |
|-------|-----------|-----------|------------|----------|
| `_analyze_sections()` | Анализ секций, определение gender | ❌ НЕТ | `monolith_v4_3_1.py:258` | Метод существует, но не вызывается |
| `_build_semantic_layers()` | Построение semantic layers | ❌ НЕТ | `monolith_v4_3_1.py:314` | Метод существует, но не вызывается |
| `annotate_text()` | Аннотация текста для UI и Suno | ❌ НЕТ | `monolith_v4_3_1.py:402` | Метод существует, но не вызывается |
| `self.style.build()` | Построение Style | ❌ НЕТ | `monolith_v4_3_1.py:237` | Метод существует, но не вызывается |
| `self.vocal_allocator.analyze()` | Аллокация вокалов | ❌ НЕТ | `monolith_v4_3_1.py:248` | Метод существует, но не вызывается |
| `self.integrity.analyze()` | Integrity сканирование | ❌ НЕТ | `monolith_v4_3_1.py:230` | Метод существует, но не вызывается |

---

## 6. Таблица: Неполная Обработка в Tone

| Параметр | Передается | Ожидается | Строка Кода | Проблема |
|----------|-----------|-----------|------------|----------|
| **Text** | ✅ `raw` | `str` | `monolith_v4_3_1.py:545` | ✅ Корректно |
| **Emotions** | ❌ Не передается | Опционально | - | Emotions не передаются в detect_key() |
| **TLP** | ❌ Не передается | Опционально | - | TLP не передается в detect_key() |
| **Key Result** | ⚠️ Частично | `Dict[str, Any]` | `monolith_v4_3_1.py:546` | Если tone_hint=None, используется FALLBACK |
| **Key Validation** | ⚠️ Частично | `str` | `monolith_v4_3_1.py:547-548` | Если key="auto" или None, используется FALLBACK |

---

## 7. Таблица: Проблемы с Optional Параметрами

| Параметр | Тип | Передается | Строка Кода | Проблема |
|----------|-----|-----------|------------|----------|
| **version** | `Optional[str]` | ✅ Передается | `monolith_v4_3_1.py:524` | ✅ Корректно, но не используется |
| **semantic_hints** | `Optional[Dict[str, Any]]` | ✅ Передается | `monolith_v4_3_1.py:525` | ✅ Корректно, но не используется |
| **preferred_gender** | `str` | ✅ Передается | `monolith_v4_3_1.py:523` | ✅ Корректно, но не используется (не вызывается _analyze_sections) |

---

## 8. Таблица: Проблемы в Rhythm Анализе

| Проблема | Строка Кода | Описание |
|----------|------------|----------|
| **TLP = None** | `monolith_v4_3_1.py:536` | TLP не рассчитывается, передается None |
| **CF = None** | `monolith_v4_3_1.py:536` | CF не рассчитывается, передается None |
| **TLP коррекция пропущена** | `rhythm.py:239` | Если `cf is None`, TLP коррекция не применяется |
| **TLP коррекция пропущена** | `rhythm.py:242` | Если `tlp` пустой, TLP коррекция не применяется |
| **Emotion weight по умолчанию** | `rhythm.py:371` | `emotion_weight=0.3` используется, но без TLP коррекция неполная |

**В rhythm.py _density_bpm():**
- Строка `239`: `if cf is not None:` - если CF=None, коррекция не применяется
- Строка `242`: `if tlp:` - если TLP пустой, коррекция не применяется
- Строка `243-246`: TLP коррекция (Pain boost, Love smoothing, Truth drive) не применяется если TLP=None

---

## 9. Таблица: Проблемы с Fallback Значениями

| Параметр | Fallback Источник | Строка Кода | Проблема |
|----------|------------------|------------|----------|
| **BPM** | `DEFAULT_CONFIG.FALLBACK_BPM` | `monolith_v4_3_1.py:537` | Используется если `global_bpm` отсутствует |
| **Key** | `DEFAULT_CONFIG.FALLBACK_KEY` | `monolith_v4_3_1.py:546-548` | Используется если key=None или "auto" |
| **Style Genre** | `DEFAULT_CONFIG.FALLBACK_STYLE` | `monolith_v4_3_1.py:557` | Всегда используется вместо расчета |
| **Style Style** | `DEFAULT_CONFIG.FALLBACK_STYLE` | `monolith_v4_3_1.py:558` | Всегда используется вместо расчета |
| **Style Visual** | `DEFAULT_CONFIG.FALLBACK_VISUAL` | `monolith_v4_3_1.py:561` | Всегда используется вместо расчета |
| **Style Narrative** | `DEFAULT_CONFIG.FALLBACK_NARRATIVE` | `monolith_v4_3_1.py:562` | Всегда используется вместо расчета |
| **Style Structure** | `DEFAULT_CONFIG.FALLBACK_STRUCTURE` | `monolith_v4_3_1.py:563` | Всегда используется вместо расчета |
| **Structure Layout** | `DEFAULT_CONFIG.FALLBACK_STRUCTURE` | `monolith_v4_3_1.py:553` | Всегда используется вместо расчета |
| **Emotion Dominant** | `DEFAULT_CONFIG.FALLBACK_EMOTION` | `monolith_v4_3_1.py:564` | Используется если `dominant` отсутствует |

**Fallback значения из config.py:**
- Строка `86`: `"FALLBACK_STYLE": "cinematic narrative"`
- Строка `87`: `"FALLBACK_KEY": "C minor"`
- Строка `88`: `"FALLBACK_BPM": 85`
- Строка `89`: `"FALLBACK_VISUAL": "soft light, calm atmosphere"`
- Строка `90`: `"FALLBACK_NARRATIVE": "introspection → tension → release"`

---

## 10. Таблица: Проблемы с Возвращаемым Результатом

| Поле Результата | Должно Содержать | Фактически Содержит | Строка Кода | Проблема |
|----------------|-----------------|-------------------|------------|----------|
| **emotions** | ✅ Полный профиль | ✅ Полный профиль | `monolith_v4_3_1.py:570` | ✅ Корректно |
| **bpm** | ✅ Рассчитанный BPM | ✅ Рассчитанный BPM | `monolith_v4_3_1.py:571` | ✅ Корректно |
| **key** | ✅ Рассчитанный Key | ⚠️ Может быть FALLBACK | `monolith_v4_3_1.py:572` | ⚠️ Частично |
| **structure** | ✅ Полная структура | ⚠️ Без layout (FALLBACK) | `monolith_v4_3_1.py:573` | ⚠️ Частично |
| **style** | ✅ Полный Style | ❌ Все FALLBACK значения | `monolith_v4_3_1.py:574` | ❌ Неполно |
| **tlp** | ✅ TLP профиль | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **rde** | ✅ RDE профиль | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **vocal** | ✅ Vocal профиль | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **color** | ✅ Color wave | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **semantic_layers** | ✅ Semantic layers | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **annotated_text_ui** | ✅ Аннотированный текст | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **annotated_text_suno** | ✅ Suno аннотация | ❌ Отсутствует | - | ❌ Не рассчитывается |
| **integrity** | ✅ Integrity профиль | ❌ Отсутствует | - | ❌ Не рассчитывается |

---

## 11. Таблица: Проблемы в core_v6.py

| Проблема | Строка Кода | Описание |
|----------|------------|----------|
| **HybridGenreEngine не используется** | `core_v6.py:45` | `self._hge` создан, но не вызывается в analyze() |
| **semantic_hints не используется** | `core_v6.py:64` | `semantic_hints` передается, но не используется в monolith |
| **version не используется** | `core_v6.py:63` | `version` передается, но не используется в monolith |

---

## 12. Таблица: Полная Цепь Проблем

| Этап | Ожидаемый Процесс | Фактический Процесс | Строка Кода | Статус |
|------|------------------|-------------------|------------|--------|
| **1. Предобработка** | Нормализация, извлечение блоков | ✅ Выполняется | `monolith_v4_3_1.py:530-531` | ✅ OK |
| **2. Emotion** | `self.emotion.analyze(raw)` | ✅ Выполняется | `monolith_v4_3_1.py:533` | ✅ OK |
| **3. TLP** | `self.tlp.analyze(raw)` | ❌ НЕ выполняется | `monolith_v4_3_1.py:536` | ❌ ПРОБЛЕМА |
| **4. Rhythm** | `self.rhythm.analyze(..., tlp=..., cf=...)` | ⚠️ Выполняется без TLP/CF | `monolith_v4_3_1.py:536` | ⚠️ НЕПОЛНО |
| **5. Tone** | `self.tone.detect_key(raw)` | ✅ Выполняется | `monolith_v4_3_1.py:545` | ✅ OK |
| **6. Sections** | `self._analyze_sections(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **7. Semantic** | `self._build_semantic_layers(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **8. Style** | `self.style.build(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **9. Genre** | Из Style или HybridGenreEngine | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **10. Vocal** | `self.vocal_allocator.analyze(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **11. Color** | `ColorEngineAdapter.resolve_color_wave(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **12. RDE** | RDE анализ | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **13. Integrity** | `self.integrity.analyze(text)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **14. Annotation** | `self.annotate_text(...)` | ❌ НЕ выполняется | - | ❌ ПРОБЛЕМА |
| **15. Сборка** | Сборка результата | ⚠️ Частичная сборка | `monolith_v4_3_1.py:569-575` | ⚠️ НЕПОЛНО |

---

## 13. Таблица: Критические Пропуски

| Критичность | Пропущенный Процесс | Влияние | Строка Кода |
|-------------|-------------------|---------|------------|
| **🔴 КРИТИЧНО** | TLP анализ | Rhythm анализ неполный (нет TLP коррекции) | `monolith_v4_3_1.py:536` |
| **🔴 КРИТИЧНО** | Style.build() | Все Style параметры = FALLBACK | `monolith_v4_3_1.py:556-565` |
| **🟡 ВАЖНО** | Section Analysis | Gender не определяется, voice hints не извлекаются | `monolith_v4_3_1.py:258` |
| **🟡 ВАЖНО** | Semantic Layers | Нет структурированных секций (Intro, Verse, Chorus) | `monolith_v4_3_1.py:314` |
| **🟡 ВАЖНО** | Text Annotation | Нет аннотированного текста для UI и Suno | `monolith_v4_3_1.py:402` |
| **🟡 ВАЖНО** | Vocal Allocator | Нет вокального профиля | `monolith_v4_3_1.py:248` |
| **🟢 СРЕДНЕ** | Color Resolution | Нет color wave | - |
| **🟢 СРЕДНЕ** | RDE анализ | Нет RDE профиля | - |
| **🟢 СРЕДНЕ** | Integrity Scan | Нет integrity проверки | `monolith_v4_3_1.py:230` |
| **🟢 СРЕДНЕ** | Hybrid Genre Engine | Нет гибридных жанров | `core_v6.py:45` |

---

## 14. Таблица: Решения Проблем

| Проблема | Решение | Строка Кода для Изменения |
|----------|--------|--------------------------|
| **TLP не рассчитывается** | Добавить `tlp = self.tlp.analyze(raw)` перед rhythm.analyze() | `monolith_v4_3_1.py:535` (после emotions) |
| **CF не рассчитывается** | Добавить `cf = tlp.get("conscious_frequency")` | `monolith_v4_3_1.py:535` (после tlp) |
| **Style.build() не вызывается** | Добавить `style_result = self.style.build(emotions, tlp, raw, bpm, semantic_hints, voice_hint)` | `monolith_v4_3_1.py:549` (после key) |
| **Section Analysis не вызывается** | Добавить `section_result = self._analyze_sections(text_blocks, preferred_gender)` | `monolith_v4_3_1.py:532` (после text_blocks) |
| **Semantic Layers не вызываются** | Добавить `semantic_layers = self._build_semantic_layers(emotions, tlp, bpm, key)` | `monolith_v4_3_1.py:549` (после style) |
| **Vocal Allocator не вызывается** | Добавить `vocal_result = self.vocal_allocator.analyze(emotions, tlp, bpm, raw)` | `monolith_v4_3_1.py:549` (после semantic_layers) |
| **Integrity не вызывается** | Добавить `integrity_result = self.integrity.analyze(raw)` | `monolith_v4_3_1.py:549` (опционально) |
| **Annotation не вызывается** | Добавить `annotated_ui, annotated_suno = self.annotate_text(text_blocks, section_profiles, semantic_sections)` | `monolith_v4_3_1.py:549` (перед return) |
| **Color не рассчитывается** | Добавить `color_result = ColorEngineAdapter().resolve_color_wave(result)` | `monolith_v4_3_1.py:549` (после style) |
| **Результат неполный** | Добавить все рассчитанные значения в return | `monolith_v4_3_1.py:569-575` |

---

## Резюме

**Критические проблемы:**
1. **TLP не рассчитывается** - строка 536, передается None в rhythm.analyze()
2. **Style.build() не вызывается** - строки 556-565, используются FALLBACK значения
3. **Section Analysis не вызывается** - метод существует, но не используется
4. **Semantic Layers не вызываются** - метод существует, но не используется
5. **Text Annotation не вызывается** - метод существует, но не используется

**Всего пропущено процессов:** 10 из 15 (66.7%)

**Всего параметров с FALLBACK:** 8 из 12 (66.7%)

---

**Создано:** Текущее состояние  
**Статус:** Полный анализ проблем процессов

