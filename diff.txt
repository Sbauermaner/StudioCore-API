diff --git a/lg.txt b/lg.txt
index 77eb0c1..69306aa 100644
--- a/lg.txt
+++ b/lg.txt
@@ -109,3 +109,7 @@ GitHub: github.com/Sbauermaner/StudioCore
 - FakeUserEngine прогнал 500 синтетических запросов с шумными командами и смешанными языками; теги и команды сохраняются без дубликатов, перевод честно остаётся выключенным, а структура и секционные заголовки заново строятся для каждого текста.
 - Устранено залипание параметров BPM/жанра: веса пересчитываются из текущего текста, ручной BPM не переносится между вызовами, а жанровая матрица получает свежие признаки для каждой выборки, что подтверждает вариативность в регрессионном фуззинге.
 - Тестовая инфраструктура дополнена проверкой идемпотентности overrides и пересчёта BPM-кривой; новая регрессия для FakeUserEngine остаётся быстрой и покрывает длинные/шумные сценарии.
+## Дополнение от 21 ноября 2025 — защита applied_overrides
+- При повторном вызове `_apply_user_overrides_once` теперь возвращается глубокая копия снимка `applied_overrides`, чтобы внешние потребители не могли нечаянно мутировать `override_debug` и переиспользовать уже применённые настройки.
+- Регрессионный тест дополнили проверкой неизменности снапшота при ручных правках результата, что страхует от скрытых утечек состояния между вызовами.
+- Повторный прогон pytest подтвердил: пайплайн остаётся стейтлес, маркеры `_overrides_applied` не просачиваются, а BPM/жанровые веса остаются вариативными.
diff --git a/lgp.txt b/lgp.txt
index 9f1b7e9..22172a6 100644
--- a/lgp.txt
+++ b/lgp.txt
@@ -4,3 +4,7 @@
 - translate_text_for_analysis стабилизировано на возврате (text, False) с очисткой хвоста файла; внутренние маркеры не просачиваются в итоговый payload.
 ## Финальный аудит StudioCore v6.4 — FAKE USER + Overrides + Statelessness
 Критических проблем не обнаружено. FAKE USER аудит подтверждает: стейтлесность соблюдена, теги и команды сохраняются корректно, веса BPM и жанров не залипают.
+## Аудит applied_overrides snapshot — защита от мутаций
+- Повторное обращение к `_apply_user_overrides_once` возвращало ссылку на `override_debug.applied_overrides`, что позволяло внешним функциям мутировать сохранённый слепок; добавлено глубокое копирование для исключения утечек состояния.
+- Тест `test_apply_user_overrides_once_idempotent_and_recalculates_bpm_curve` теперь фиксирует неизменность слепка даже при попытке ручной правки результата.
+- Критических проблем не обнаружено. FAKE USER аудит подтверждает: стейтлесность соблюдена, теги и команды сохраняются корректно, веса BPM и жанров не залипают.
diff --git a/studiocore/core_v6.py b/studiocore/core_v6.py
index dc5c3e5..0bc0e3c 100644
--- a/studiocore/core_v6.py
+++ b/studiocore/core_v6.py
@@ -1057,7 +1057,10 @@ class StudioCoreV6:
     ) -> Dict[str, Any]:
         if payload.get("_overrides_applied"):
             debug_info = payload.get("override_debug", {})
-            return debug_info.get("applied_overrides", {}) if isinstance(debug_info, dict) else {}
+            applied = (
+                debug_info.get("applied_overrides", {}) if isinstance(debug_info, dict) else {}
+            )
+            return copy.deepcopy(applied)
 
         adjustments: Dict[str, Any] = {}
 
diff --git a/tests/test_overrides_once.py b/tests/test_overrides_once.py
index 3aae2b3..a4e79db 100644
--- a/tests/test_overrides_once.py
+++ b/tests/test_overrides_once.py
@@ -31,3 +31,9 @@ def test_apply_user_overrides_once_idempotent_and_recalculates_bpm_curve():
     assert adjustments_second == adjustments_first
     assert payload == snapshot_before_second
 
+    adjustments_second["bpm"]["estimate"] = 130.0
+    assert (
+        payload["override_debug"]["applied_overrides"]["bpm"]["estimate"]
+        == adjustments_first["bpm"]["estimate"]
+    )
+
