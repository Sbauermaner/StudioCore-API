# -*- coding: utf-8 -*-
"""
📘 StudioCore README Template Generator
--------------------------------------
Создаёт или восстанавливает README.md с базовой структурой,
если файл отсутствует или повреждён.
"""

from datetime import datetime
from pathlib import Path
import os

# === CONFIG ===
STUDIO_VERSION = "v5.0"
README_PATH = Path("README.md")
SERVER_URL = "https://sbauer8-studiocore-api.hf.space"

def generate_readme():
    """Создаёт базовый README.md, если отсутствует."""
    content = f"""---
title: StudioCore {STUDIO_VERSION} — Expressive Adaptive Engine
emoji: 🎧
colorFrom: blue
colorTo: pink
sdk: docker
app_file: app.py
pinned: true
---

# 🎧 StudioCoreAPI by Bauer
> 🟢 **API initializing...** | Created: {datetime.utcnow().strftime('%Y-%m-%d %H:%M:%S UTC')} | Version: `{STUDIO_VERSION}`

---

## 💡 Описание

**StudioCore** — это интеллектуальное ядро для анализа текста, эмоций и структуры,  
создающее **адаптивные музыкальные промты** под Suno, Sora и другие ИИ-системы.  

Модуль возвращает четыре результата:
1. 📊 **Краткий анализ** — жанр, BPM, философия, версия.  
2. 🎼 **Полный промт** — структура, вокал, инструменты.  
3. 🎧 **Suno-промт (до 1000 символов)** — для генерации музыки.  
4. 📜 **Аннотированный текст** — автоматическая структура (Intro, Verse, Chorus, Bridge, Outro).

---

## 🚀 Как использовать

1. Введите текст песни, стихотворения или манифеста.  
2. (Опционально) укажите стиль или тип вокала.  
3. Нажмите **Run** — StudioCore выполнит анализ и сформирует промт.

---

## 🧠 Основные модули

- Emotion Analyzer — анализ эмоциональных слоёв (радость, грусть, гнев, покой).  
- Truth × Love × Pain Engine — философская матрица оценки смысла.  
- Rhythm Engine — определение темпа и метрики.  
- Frequency Engine — безопасный резонансный профиль (RNS-Safe).  
- ToneSync Engine — визуальные и частотные соответствия.  
- Style Matrix — жанр, тональность, вокальные техники.  
- Vocal Registry — подбор ансамбля (solo, duet, choir).  
- Annotation Layer — секции и эмоциональная динамика.

---

## ⚙️ API Endpoint

**POST** `{SERVER_URL}/api/predict`

### Пример вызова (Python)
```python
from gradio_client import Client

client = Client("{SERVER_URL}")
result = client.predict(
    text=\"\"\"Вся моя жизнь — как быль или небыль...\"\"\",
    api_name="/predict"
)
print(result)
```

---

## 📦 Формат ответа

Ответ возвращает JSON со следующими ключами:

| Ключ | Описание |
| ---- | -------- |
| `summary` | Краткий сводный анализ |
| `prompt` | Подготовленный Suno-промт |
| `annotations` | Секционные аннотации |
| `meta` | Версия StudioCore и диагностическая информация |

---

## 🧩 Автоматическое восстановление README

Скрипт можно запустить вручную:

```bash
python readme_template_gen.py
```

Если README.md отсутствует, он будет создан. Если файл существует, содержимое перезапишется свежей версией шаблона.

---

_Последнее обновление:_ {datetime.utcnow().strftime('%Y-%m-%d %H:%M:%S UTC')}
"""

    README_PATH.write_text(content, encoding="utf-8")
    return README_PATH


if __name__ == "__main__":
    path = generate_readme()
    print(f"README template written to {path.resolve()}")
