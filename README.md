# StudioCore IMMORTAL — Текущий срез (v6 совместимость + Monolith v4.3.11)

## 📑 Содержание
- [Обзор проекта](#-обзор-проекта)
- [Философия (Truth × Love × Pain = Conscious Frequency)](#-философия-truth--love--pain--conscious-frequency)
- [Архитектура и монолиты](#-архитектура-и-монолиты)
- [Основные функции и движки](#-основные-функции-и-движки)
- [Текущий формат вывода](#-текущий-формат-вывода)
- [Известные проблемы и баги](#-известные-проблемы-и-баги)
- [Оптимизация под Suno v5](#-оптимизация-под-suno-v5)
- [Лицензия и ограничения](#-лицензия-и-ограничения)
- [Как запустить локально](#-как-запустить-локально)

## 🎛️ Обзор проекта
StudioCore IMMORTAL держит совместимость с веткой v6 и подключает устоявшийся монолит StudioCore v4.3.11. Код предоставляет пайплайн анализа текста → эмоции → ритм → стиль → аннотации Suno. Текущая реализация ориентирована на стабильность, а не на новые функции v7.

## 🧠 Философия (Truth × Love × Pain = Conscious Frequency)
Движки строят эмоциональную карту по трем осям Truth, Love, Pain, выводя производную Conscious Frequency. Эта философия используется для согласования BPM, тональности, вокала, жанровых смещений и семантических секций.

## 🏗️ Архитектура и монолиты
- **core_v6.py — StudioCoreV6**: обертка, соединяющая новые логические движки (BPM, REM, Genre Router, TLP и др.) с монолитом v4.3.11. При сбое Fusion/Suno ошибки складываются в `diagnostics` без падения пайплайна.
- **monolith_v4_3_1.py — StudioCore (v4.3.11)**: основной анализатор эмоций/TLP, стиль-матрица, ритмический анализ и построение Suno-аннотаций. Используется как fallback/совместимость.
- Цепочка выполнения: StudioCoreV6 собирает структуру и семантические подсказки → вызывает монолит для базовой оценки → дополняет данными v6 (динамика эмоций, BPM-кривая, тональность, RNS safety, инструментальная палитра) → возвращает итог с диагностикой.

## 🚀 Основные функции и движки
- **Эмоции и TLP**: LegacyEmotionEngine + TruthLovePainEngine, динамический EmotionMatrix и MultimodalEmotionMatrix.
- **Ритм и BPM**: BPMEngine + RhythmDynamicsEmotionEngine, перерасчет BPM-кривой, командный интерпретатор BPM.
- **Вокал**: VocalEngine в v6 и вокальный анализ секций в монолите (выявление пола/формы вокала, распределение по секциям).
- **Жанры**: DynamicGenreRouter + GenreMatrixExtended, адаптация макро/сабжанров, базовая маршрутизация под стиль.
- **Тональность и тембр**: TonalityEngine, ToneSyncEngine, ColorEmotionEngine/Adapter, UniversalFrequencyEngine с RNSSafety.
- **Секции и структура**: TextStructureEngine, SectionParser, SectionIntelligenceEngine + semantic layers (Intro/Verse/Bridge/Chorus/Outro).
- **Аннотации**: LyricsAnnotationEngine/FANFAnnotationEngine (v6) и Suno-аннотации монолита для стилевого и лирического промптов.

## 🧾 Текущий формат вывода
Монолит генерирует два текстовых блока аннотаций:
- `annotated_text_ui`: строки вида `[VERSE - MALE - reflective, mid, standard, BPM≈128]` перед каждым фрагментом текста, затем закрывающий тег `[End – BPM≈<число>, Tone=<тональность>]`.
- `annotated_text_suno`: аналогичный минималистичный формат `[VERSE - vocal: MALE - mood: reflective - energy: mid - arrangement: standard]` + финальный тег `[<BPM> BPM, <тональность>]`.

StudioCoreV6 дополнительно прокидывает вычисленные словари (`style`, `bpm`, `tonality`, `instrumentation`, `diagnostics`, `suno_prompt`, `fusion_summary`, `fanf`).

## 🐞 Известные проблемы и баги
- Отсутствие FusionEngineV64 или его падения приводят только к предупреждению в `diagnostics`, финальный BPM/жанр/тональность может не синхронизироваться с Fusion-результатами. Suno prompt также опционален и может отсутствовать при ошибке импорта/вызова. 
- Монолит продолжает выводить старый формат тегов (v6.4 стиль) вместо более лаконичного v6.5/v7; теги содержат жёстко прошитые поля mood/energy/arrangement.
- Гендерный детектор в секционном анализе ограничен простыми regex и легко ошибается на сложной морфологии или многоязычных текстах.
- Семантические слои и BPM/тональность секций используют фиксированные коэффициенты (предопределённые коэффициенты TLP), что может давать неточные значения для жанров вне базовой карты.
- Диагностика Suno/Fusion не пробрасывается наружу в виде исключений или явных статусов API — интегратору приходится вручную проверять поле `diagnostics`.
- Нет нормализованного кеширования: каждая сессия пересоздаёт движки, а Fusion/Suno импортируются лениво при каждом вызове.

## 🎛️ Оптимизация под Suno v5
- Поддерживается генерация двух промптов: `prompt_suno_style` (стилевой) и `prompt_suno_lyrics` (лирика). 
- Формат тегов в `annotated_text_suno` остаётся совместимым с Suno v5, но не использует новые поля v5.2+ (визуальные подсказки, negative prompts). 
- При отсутствии FusionEngineV64 или adapter.build_suno_prompt Suno-поля будут пусты, только предупреждения в `diagnostics`.

## 📜 Лицензия и ограничения
Код снабжён блоком **AI_TRAINING_PROHIBITED**: использование для обучения моделей запрещено без письменного разрешения автора. Основная лицензия — Enhanced MIT (см. NOTICE/NOTICE/LICENSE в репозитории).

## 🖥️ Как запустить локально
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py  # запускает FastAPI/Gradio адаптер, если он настроен
```

> FusionEngineV64 и внешние зависимости Suno по умолчанию могут отсутствовать; в таком случае движок вернёт предупреждения в `diagnostics` и продолжит работу без Fusion/Suno промптов.
