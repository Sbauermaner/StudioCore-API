# Аудит файлов в папке `tests`

**Дата:** 2025  
**Цель:** Проверка правильности работы всех тестовых файлов относительно текущей структуры проекта

---

## 📊 Сводная таблица

| Файл | Синтаксис | Импорты | Логика | Статус | Проблемы |
|------|-----------|---------|--------|--------|----------|
| `conftest.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |
| `test_loader.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |
| `test_text_utils.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |
| `test_section_parser.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |
| `test_core_v6_pipeline.py` | ✅ OK | ✅ OK | ❌ ОШИБКА | ❌ НЕ РАБОТАЕТ | Строки 34, 38 |
| `test_stateless_core.py` | ✅ OK | ✅ OK | ❌ ОШИБКА | ❌ НЕ РАБОТАЕТ | Строка 11 |
| `test_state_persistence.py` | ✅ OK | ✅ OK | ❌ ОШИБКА | ❌ НЕ РАБОТАЕТ | Строки 27-30, 42-64, 74-88, 134-136 |
| `test_integration_v7.py` | ✅ OK | ✅ OK | ❌ ОШИБКА | ❌ НЕ РАБОТАЕТ | Строки 42-46, 53-56, 61-64, 67-73, 115-116 |
| `test_full_system_diagnostics.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |
| `auto_calibration.py` | ✅ OK | ✅ OK | ✅ OK | ✅ РАБОТАЕТ | Нет |

---

## ✅ Файлы, которые работают правильно

### 1. `conftest.py` (17 строк)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строки 6-11:** Правильная настройка пути к проекту
  ```python
  PROJECT_ROOT = pathlib.Path(__file__).resolve().parent.parent
  if str(PROJECT_ROOT) not in sys.path:
      sys.path.insert(0, str(PROJECT_ROOT))
  ```
- **Строки 1-16:** Корректные signature blocks

**Проблемы:** ❌ Нет

---

### 2. `test_loader.py` (34 строки)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строки 6-12:** Правильные импорты из `studiocore`
  ```python
  from studiocore import (
      LOADER_STATUS,
      MONOLITH_VERSION,
      STUDIOCORE_VERSION,
      get_core,
      loader_diagnostics,
  )
  ```
- **Строки 15-18:** Тест `test_get_core_produces_instance()` - проверяет наличие метода `analyze` и статус загрузчика
- **Строки 21-27:** Тест `test_loader_diagnostics_shape()` - проверяет структуру диагностики

**Проблемы:** ❌ Нет

---

### 3. `test_text_utils.py` (28 строк)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строка 3:** Правильный импорт `from studiocore.text_utils import translate_text_for_analysis`
- **Строки 6-18:** Тест `test_translate_text_for_analysis_passthrough_languages()` - проверяет поддержку языков ru/en/multilingual
- **Строки 21-27:** Тест `test_translate_text_for_analysis_simulates_for_unsupported()` - проверяет симуляцию перевода для неподдерживаемых языков

**Проблемы:** ❌ Нет

---

### 4. `test_section_parser.py` (35 строк)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строка 6:** Правильный импорт `from studiocore.section_parser import SectionParser`
- **Строки 9-14:** Корректный пример текста с секциями
- **Строки 17-28:** Тест `test_section_parser_detects_annotations()` - проверяет парсинг секций и применение аннотаций

**Проблемы:** ❌ Нет

---

### 5. `test_full_system_diagnostics.py` (63 строки)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строки 6-11:** Правильная настройка пути к проекту
- **Строки 14-23:** Тест `test_full_system_diagnostics_passes_for_valid_workflows()` - проверяет успешное выполнение диагностики
- **Строки 26-56:** Тест `test_full_system_diagnostics_fails_on_invalid_workflow()` - проверяет обработку ошибок в workflow файлах

**Проблемы:** ❌ Нет

---

### 6. `auto_calibration.py` (231 строка)

**Статус:** ✅ **РАБОТАЕТ ПРАВИЛЬНО**

**Что работает:**
- **Строки 16-18:** Правильная настройка пути к проекту
- **Строки 21-71:** Тест `test_hybrid_genre_engine()` - проверяет работу HybridGenreEngine
- **Строки 73-111:** Тест `test_missing_modules()` - проверяет наличие модулей
- **Строки 114-151:** Тест `test_circular_imports()` - проверяет отсутствие циклических импортов
- **Строки 154-185:** Тест `test_core_v6_initialization()` - проверяет инициализацию CoreV6

**Проблемы:** ❌ Нет

---

## ❌ Файлы, которые работают неправильно

### 1. `test_core_v6_pipeline.py` (52 строки)

**Статус:** ❌ **НЕ РАБОТАЕТ**

**Что работает:**
- **Строки 1-4:** Корректные signature blocks
- **Строка 6:** Правильный импорт `from studiocore.core_v6 import StudioCoreV6`
- **Строки 9-15:** Корректный пример текста
- **Строки 18-28:** Тест `test_core_v6_analyze_produces_expected_sections()` - работает правильно
- **Строки 41-45:** Тест `test_core_v6_provides_diagnostics()` - работает правильно

**Что не работает:**
- **Строка 34:** `core.instrument_dynamics.map_instruments_to_structure = lambda *args, **kwargs: None`
  - **Проблема:** `StudioCoreV6` не имеет атрибута `instrument_dynamics`
  - **Причина:** `StudioCoreV6` - это обертка вокруг `_core` (monolith), и `instrument_dynamics` находится внутри `_core`, а не напрямую в `StudioCoreV6`
  - **Исправление:** Нужно использовать `core._core.instrument_dynamics` или изменить тест

- **Строка 38:** `assert isinstance(result["suno_annotations"], list)`
  - **Проблема:** Может не работать, если `instrument_dynamics` не был правильно мокирован
  - **Причина:** Зависит от предыдущей строки

**Рекомендации:**
1. Изменить строку 34 на: `core._core.instrument_dynamics.map_instruments_to_structure = lambda *args, **kwargs: None`
2. Или использовать mock для `instrument_dynamics`

---

### 2. `test_stateless_core.py` (12 строк)

**Статус:** ❌ **НЕ РАБОТАЕТ**

**Что работает:**
- **Строка 1:** Правильный импорт `from studiocore.core_v6 import StudioCoreV6`
- **Строки 4-10:** Тест `test_stateless_analysis_leak()` - логика проверки правильная

**Что не работает:**
- **Строка 11:** `assert core.annotate_ui(p1) != core.annotate_ui(p2)`
  - **Проблема:** `StudioCoreV6` не имеет метода `annotate_ui()`
  - **Причина:** `annotate_ui()` находится в monolith (`_core`), но не экспортируется через `StudioCoreV6`
  - **Исправление:** Нужно использовать `core._core.annotate_ui()` или добавить метод `annotate_ui()` в `StudioCoreV6`

**Рекомендации:**
1. Изменить строку 11 на: `assert core._core.annotate_ui(p1) != core._core.annotate_ui(p2)`
2. Или добавить метод `annotate_ui()` в `StudioCoreV6` как прокси к `_core.annotate_ui()`

---

### 3. `test_state_persistence.py` (141 строка)

**Статус:** ❌ **НЕ РАБОТАЕТ**

**Что работает:**
- **Строки 1-6:** Корректное описание теста
- **Строки 8-15:** Правильные импорты и настройка пути
- **Строки 21-23:** Правильная инициализация `StudioCoreV6`
- **Строки 32-38:** Тест `test_system_components_preserved_after_first_analyze()` - логика правильная
- **Строки 66-88:** Тест `test_system_components_preserved_after_multiple_analyze()` - логика правильная
- **Строки 90-100:** Тест `test_engine_bundle_cleared_after_analyze()` - логика правильная
- **Строки 102-117:** Тест `test_no_attribute_error_on_reuse()` - работает правильно
- **Строки 119-137:** Тест `test_different_text_inputs()` - логика правильная

**Что не работает:**
- **Строки 27-30:** Попытка доступа к несуществующим атрибутам:
  ```python
  self.initial_text_engine = self.core._text_engine
  self.initial_section_parser = self.core._section_parser
  self.initial_emotion_engine = self.core._emotion_engine
  self.initial_bpm_engine = self.core._bpm_engine
  ```
  - **Проблема:** `StudioCoreV6` не имеет этих атрибутов. У него есть только `_core` и `_hge`
  - **Причина:** Эти атрибуты находятся внутри `_core` (monolith), но не экспортируются напрямую

- **Строки 42-64:** Проверка сохранения несуществующих атрибутов:
  ```python
  self.assertIs(self.core._text_engine, self.initial_text_engine, ...)
  self.assertIs(self.core._section_parser, self.initial_section_parser, ...)
  self.assertIs(self.core._emotion_engine, self.initial_emotion_engine, ...)
  self.assertIs(self.core._bpm_engine, self.initial_bpm_engine, ...)
  ```
  - **Проблема:** Те же атрибуты не существуют

- **Строки 74-88:** Аналогичная проблема с проверкой сохранения атрибутов

- **Строки 134-136:** Проверка наличия несуществующих атрибутов:
  ```python
  self.assertIsNotNone(self.core._hge)  # ✅ OK
  self.assertIsNotNone(self.core._text_engine)  # ❌ НЕ СУЩЕСТВУЕТ
  self.assertIsNotNone(self.core._section_parser)  # ❌ НЕ СУЩЕСТВУЕТ
  ```

**Рекомендации:**
1. Изменить тесты для работы с `_core` вместо прямых атрибутов:
   - `self.core._core.emotion` вместо `self.core._emotion_engine`
   - `self.core._core.tlp` вместо `self.core._text_engine`
   - И т.д.
2. Или добавить свойства-прокси в `StudioCoreV6` для доступа к этим атрибутам

---

### 4. `test_integration_v7.py` (146 строк)

**Статус:** ❌ **НЕ РАБОТАЕТ**

**Что работает:**
- **Строки 1-9:** Корректное описание теста
- **Строки 11-25:** Правильные импорты и настройка пути
- **Строки 28-33:** Правильная инициализация `StudioCoreV6`
- **Строки 35-46:** Тест `test_state_persistence()` - логика правильная, но проверяет несуществующие атрибуты
- **Строки 75-99:** Тест `test_configuration_loading()` - работает правильно
- **Строки 101-117:** Тест `test_multiple_analyze_calls()` - логика правильная, но проверяет несуществующие атрибуты
- **Строки 118-134:** Тест `test_genre_weights_structure()` - работает правильно
- **Строки 136-141:** Тест `test_thresholds_structure()` - работает правильно

**Что не работает:**
- **Строки 42-46:** Проверка несуществующих атрибутов:
  ```python
  self.assertIsNotNone(self.core._hge, "_hge should be preserved")  # ✅ OK
  self.assertIsNotNone(self.core._text_engine, "_text_engine should be preserved")  # ❌
  self.assertIsNotNone(self.core._emotion_engine, "_emotion_engine should be preserved")  # ❌
  ```

- **Строки 53-56:** Аналогичная проблема

- **Строки 61-64:** Проверка инициализации несуществующих атрибутов:
  ```python
  self.assertIsNotNone(self.core._text_engine)  # ❌
  self.assertIsNotNone(self.core._emotion_engine)  # ❌
  self.assertIsNotNone(self.core._bpm_engine)  # ❌
  self.assertIsNotNone(self.core._section_parser)  # ❌
  ```

- **Строки 67-73:** Проверка повторного использования несуществующих атрибутов:
  ```python
  engines1 = self.core._text_engine  # ❌
  self.core.analyze("Test")
  engines2 = self.core._text_engine  # ❌
  ```

- **Строки 115-116:** Проверка сохранения несуществующих атрибутов:
  ```python
  self.assertIsNotNone(self.core._hge)  # ✅ OK
  self.assertIsNotNone(self.core._text_engine)  # ❌
  ```

**Рекомендации:**
1. Изменить все проверки для работы с `_core`:
   - `self.core._core.emotion` вместо `self.core._emotion_engine`
   - `self.core._core.tlp` вместо `self.core._text_engine`
   - И т.д.
2. Или добавить свойства-прокси в `StudioCoreV6`

---

## 📋 Детальная таблица проблем

| Файл | Строка | Проблема | Текущий код | Правильный код |
|------|--------|----------|-------------|----------------|
| `test_core_v6_pipeline.py` | 34 | Доступ к несуществующему атрибуту | `core.instrument_dynamics` | `core._core.instrument_dynamics` |
| `test_stateless_core.py` | 11 | Вызов несуществующего метода | `core.annotate_ui()` | `core._core.annotate_ui()` |
| `test_state_persistence.py` | 27-30 | Доступ к несуществующим атрибутам | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_state_persistence.py` | 42-64 | Проверка несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_state_persistence.py` | 74-88 | Проверка несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_state_persistence.py` | 134-136 | Проверка несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_integration_v7.py` | 42-46 | Проверка несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_integration_v7.py` | 53-56 | Проверка несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_integration_v7.py` | 61-64 | Проверка инициализации несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_integration_v7.py` | 67-73 | Проверка повторного использования несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |
| `test_integration_v7.py` | 115-116 | Проверка сохранения несуществующих атрибутов | `self.core._text_engine` и др. | `self.core._core.emotion` и др. |

---

## 🔍 Анализ структуры StudioCoreV6

**Текущая структура `StudioCoreV6`:**
```python
class StudioCoreV6:
    def __init__(self, ...):
        self._core = get_core(...)  # Monolith instance
        self._hge = HybridGenreEngine()  # Optional
```

**Атрибуты, которые есть:**
- ✅ `_core` - экземпляр monolith
- ✅ `_hge` - HybridGenreEngine (может быть None)

**Атрибуты, которых нет (но есть в `_core`):**
- ❌ `_text_engine` → `_core.tlp` или `_core.emotion`
- ❌ `_emotion_engine` → `_core.emotion`
- ❌ `_bpm_engine` → `_core.freq` или `_core.bpm`
- ❌ `_section_parser` → `_core.rhythm` или отдельный парсер
- ❌ `instrument_dynamics` → `_core.instrument_dynamics` (если есть)
- ❌ `annotate_ui()` → `_core.annotate_ui()` (если есть)

---

## ✅ Рекомендации по исправлению

### Вариант 1: Изменить тесты для работы с `_core`
- Заменить все обращения к несуществующим атрибутам на обращения через `_core`
- Пример: `self.core._text_engine` → `self.core._core.emotion`

### Вариант 2: Добавить свойства-прокси в `StudioCoreV6`
- Добавить свойства для доступа к внутренним компонентам `_core`
- Пример:
  ```python
  @property
  def _text_engine(self):
      return self._core.emotion
  
  @property
  def _emotion_engine(self):
      return self._core.emotion
  ```

### Вариант 3: Добавить методы-прокси
- Добавить методы, которые делегируют вызовы в `_core`
- Пример:
  ```python
  def annotate_ui(self, result):
      return self._core.annotate_ui(result)
  ```

---

**Создано:** Аудит тестовых файлов  
**Статус:** ✅ 6 файлов работают правильно, ❌ 4 файла требуют исправления

